diff --git a/player/command.c b/player/command.c
index 96bc06a0b0..af000cc79e 100644
--- a/player/command.c
+++ b/player/command.c
@@ -85,6 +85,13 @@
 #include <windows.h>
 #endif
 
+#ifdef __ANDROID__
+#include <android/log.h>
+#define MPVLOG(...) __android_log_print(ANDROID_LOG_ERROR, "MPVTRACK", __VA_ARGS__)
+#else
+#define MPVLOG(...) do {} while (0)
+#endif
+
 struct command_ctx {
     // All properties, terminated with a {0} item.
     struct m_property *properties;
@@ -1948,6 +1955,7 @@ static struct track* track_next(struct MPContext *mpctx, enum stream_type type,
 static int mp_property_switch_track(void *ctx, struct m_property *prop,
                                     int action, void *arg)
 {
+    MPVLOG("AAA switch_track property request");
     MPContext *mpctx = ctx;
     const int *def = prop->priv;
     int order = def[0];
@@ -6451,11 +6459,16 @@ static void cmd_rescan_external_files(void *p)
     if (!cmd->args[0].v.i && mpctx->playback_initialized) {
         // somewhat fuzzy and not ideal
         struct track *a = select_default_track(mpctx, 0, STREAM_AUDIO);
-        if (a && a->is_external)
+        if (a && a->is_external) {
+            MPVLOG("AAA 1select_default_track called for subtitles");
             mp_switch_track(mpctx, STREAM_AUDIO, a, 0);
+        }
+        MPVLOG("AAA 2select_default_track called for subtitles");
         struct track *s = select_default_track(mpctx, 0, STREAM_SUB);
-        if (s && s->is_external)
+        if (s && s->is_external) {
+          MPVLOG("AAA 3select_default_track called for subtitles");
             mp_switch_track(mpctx, STREAM_SUB, s, 0);
+        }
 
         print_track_list(mpctx, "Track list:");
     }
diff --git a/player/loadfile.c b/player/loadfile.c
index f9b653185b..02ada17135 100644
--- a/player/loadfile.c
+++ b/player/loadfile.c
@@ -61,6 +61,13 @@
 #include "core.h"
 #include "command.h"
 
+#ifdef __ANDROID__
+#include <android/log.h>
+#define MPVLOG(...) __android_log_print(ANDROID_LOG_ERROR, "MPVTRACK", __VA_ARGS__)
+#else
+#define MPVLOG(...) do {} while (0)
+#endif
+
 // Called from the demuxer thread if a new packet is available, or other changes.
 static void wakeup_demux(void *pctx)
 {
@@ -173,7 +180,7 @@ static void kill_demuxers_reentrant(struct MPContext *mpctx,
             } else if (wait < 0) {
                 demux_free_async_force(item);
                 if (!force)
-                    MP_VERBOSE(mpctx, "Forcefully terminating demuxers...\n");
+                    MPVLOG("AAA Forcefully terminating demuxers...\n");
                 force = true;
             }
         }
@@ -579,8 +586,17 @@ struct track *select_default_track(struct MPContext *mpctx, int order,
                              NULL;
     bool sub = type == STREAM_SUB;
     struct track *pick = NULL;
-    for (int n = 0; n < mpctx->num_tracks; n++) {
+    for (int n = 0; n < mpctx->num_tracks; n++) { 
         struct track *track = mpctx->tracks[n];
+        if (track->type == STREAM_SUB) {
+            MPVLOG("AAAA default-track candidate: id=%d selected=%d default=%d forced=%d external=%d codec=%s",
+                   track->user_tid,
+                   track->selected,
+                   track->default_track,
+                   track->forced_track,
+                   track->is_external,
+                   track->stream && track->stream->codec ? track->stream->codec->codec : "null");
+        }        
         if (track->type != type)
             continue;
         if (track->user_tid == tid) {
@@ -727,6 +743,7 @@ void mp_switch_track_n(struct MPContext *mpctx, int order, enum stream_type type
         uninit_sub(mpctx, current);
 
     if (current) {
+        MPVLOG("AAA current->selected = false");
         current->selected = false;
         reselect_demux_stream(mpctx, current, false);
     }
@@ -734,6 +751,7 @@ void mp_switch_track_n(struct MPContext *mpctx, int order, enum stream_type type
     mpctx->current_track[order][type] = track;
 
     if (track) {
+        MPVLOG("AAA current->selected = true");
         track->selected = true;
         reselect_demux_stream(mpctx, track, false);
     }
@@ -1139,7 +1157,7 @@ static MP_THREAD_VOID open_demux_thread(void *ctx)
     mpctx->open_res_demuxer = demux;
 
     if (demux) {
-        MP_VERBOSE(mpctx, "Opening done: %s\n", mpctx->open_url);
+        MPVLOG("AAA Opening done: %s\n", mpctx->open_url);
 
         if (mpctx->open_for_prefetch && !demux->fully_read) {
             int num_streams = demux_get_num_stream(demux);
@@ -1153,7 +1171,7 @@ static MP_THREAD_VOID open_demux_thread(void *ctx)
             demux_start_prefetch(demux);
         }
     } else {
-        MP_VERBOSE(mpctx, "Opening failed or was aborted: %s\n", mpctx->open_url);
+        MPVLOG("AAA Opening failed or was aborted: %s\n", mpctx->open_url);
 
         if (p.demuxer_failed) {
             mpctx->open_res_error = MPV_ERROR_UNKNOWN_FORMAT;
@@ -1223,21 +1241,21 @@ static void open_demux_reentrant(struct MPContext *mpctx)
         bool correct_url = strcmp(mpctx->open_url, url) == 0;
 
         if (correct_url && !mpctx->demuxer_changed && !failed) {
-            MP_VERBOSE(mpctx, "Using prefetched/prefetching URL.\n");
+            MPVLOG("AAA Using prefetched/prefetching URL.\n");
         } else {
             if (correct_url && failed) {
-                MP_VERBOSE(mpctx, "Prefetched URL failed, retrying.\n");
+                MPVLOG("AAA Prefetched URL failed, retrying.\n");
             } else if (mpctx->demuxer_changed) {
                 if (done) {
-                    MP_VERBOSE(mpctx, "Dropping finished prefetch because demuxer options changed.\n");
+                    MPVLOG("AAA Dropping finished prefetch because demuxer options changed.\n");
                 } else {
-                    MP_VERBOSE(mpctx, "Aborting ongoing prefetch because demuxer options changed.\n");
+                    MPVLOG("AAA Aborting ongoing prefetch because demuxer options changed.\n");
                 }
             } else {
                 if (done) {
-                    MP_VERBOSE(mpctx, "Dropping finished prefetch of wrong URL.\n");
+                    MPVLOG("AAA Dropping finished prefetch of wrong URL.\n");
                 } else {
-                    MP_VERBOSE(mpctx, "Aborting ongoing prefetch of wrong URL.\n");
+                    MPVLOG("AAA Aborting ongoing prefetch of wrong URL.\n");
                 }
             }
             cancel_open(mpctx);
@@ -1280,7 +1298,7 @@ void prefetch_next(struct MPContext *mpctx)
 
     struct playlist_entry *new_entry = mp_next_file(mpctx, +1, false, false);
     if (new_entry && new_entry->filename) {
-        MP_VERBOSE(mpctx, "Prefetching: %s\n", new_entry->filename);
+        MPVLOG("AAA Prefetching: %s\n", new_entry->filename);
         start_open(mpctx, new_entry->filename, new_entry->stream_flags, true);
     }
 }
@@ -1333,6 +1351,7 @@ static void cleanup_deassociated_complex_filters(struct MPContext *mpctx)
                 talloc_free(track->dec->f);
                 track->dec = NULL;
             }
+            MPVLOG("AAA track->selected = false;");
             track->selected = false;
         }
     }
@@ -1352,7 +1371,7 @@ static void cleanup_deassociated_complex_filters(struct MPContext *mpctx)
 static void kill_outputs(struct MPContext *mpctx, struct track *track)
 {
     if (track->vo_c || track->ao_c) {
-        MP_VERBOSE(mpctx, "deselecting track %d for lavfi-complex option\n",
+        MPVLOG("AAA deselecting track %d for lavfi-complex option\n",
                    track->user_tid);
         mp_switch_track(mpctx, track->type, NULL, 0);
     }
@@ -1860,7 +1879,7 @@ static void play_current_file(struct MPContext *mpctx)
     if (!mpctx->vo_chain)
         handle_force_window(mpctx, true);
 
-    MP_VERBOSE(mpctx, "Starting playback...\n");
+    MPVLOG("AAA Starting playback...\n");
 
     mpctx->playback_initialized = true;
     mpctx->playing->playlist_prev_attempt = false;
@@ -1918,7 +1937,7 @@ static void play_current_file(struct MPContext *mpctx)
         run_playloop(mpctx);
     mpctx->in_playloop = false;
 
-    MP_VERBOSE(mpctx, "EOF code: %d  \n", mpctx->stop_play);
+    MPVLOG("AAA EOF code: %d  \n", mpctx->stop_play);
 
 terminate_playback:
 
@@ -1999,7 +2018,7 @@ terminate_playback:
     };
     mp_notify(mpctx, MPV_EVENT_END_FILE, &end_event);
 
-    MP_VERBOSE(mpctx, "finished playback, %s (reason %d)\n",
+    MPVLOG("AAA finished playback, %s (reason %d)\n",
                mpv_error_string(end_event.error), end_event.reason);
     if (end_event.error == MPV_ERROR_UNKNOWN_FORMAT)
         MP_ERR(mpctx, "Failed to recognize file format.\n");
@@ -2083,11 +2102,11 @@ void mp_play_files(struct MPContext *mpctx)
 
     // Wait for all scripts to load before possibly starting playback.
     if (!mp_clients_all_initialized(mpctx)) {
-        MP_VERBOSE(mpctx, "Waiting for scripts...\n");
+        MPVLOG("AAA Waiting for scripts...\n");
         while (!mp_clients_all_initialized(mpctx))
             mp_idle(mpctx);
         mp_wakeup_core(mpctx); // avoid lost wakeups during waiting
-        MP_VERBOSE(mpctx, "Done loading scripts.\n");
+        MPVLOG("AAA Done loading scripts.\n");
     }
     // After above is finished; but even if it's skipped.
     mp_msg_set_early_logging(mpctx->global, false);
